// function and the parametor in the java scripe and the  method  tyeps 
function myfun(num, num1) {   // functuin in the reference 
    const x = num + num1;
    return x;
}
// execute the function 
const y = myfun(10, 40); // passm the prarametor 
// console.log(y);

// function in the lecture two 

function addCash(...casha) {  // reste operator and  seprete operator 
    return casha;
}
//  console.log (addCash(200, 3333, 4545, 333,333));
// pass the objecte in  function 
user = {
    name: "vishal ",
    id: 333,
    price: 3000,

}
// pass the objecte in function 
function pass_Price(anyobj) {
    // console.log(`${anyobj.id}`${anyobj.name});
}

const myarr = [34, 44, 4, 3, 456, 66, 66];
function returnarray1(getarr) {
    return getarr[1];
}


